2025-11-12T01:12:28.547589Z  INFO nobodywho::chat: Using forced tool calling mode
2025-11-12T01:12:28.547681Z  INFO nobodywho::chat: Injecting manual_tool_prefix into response: 92 chars
2025-11-12T01:12:29.177375Z  INFO nobodywho::chat: Worker writing until done
Grammar still awaiting trigger after token 151667 (`<think>`)
Grammar still awaiting trigger after token 198 (`
`)
Grammar still awaiting trigger after token 32313 (`Okay`)
Grammar still awaiting trigger after token 11 (`,`)
Grammar still awaiting trigger after token 279 (` the`)
Grammar still awaiting trigger after token 1196 (` user`)
Grammar still awaiting trigger after token 1053 (` said`)
Grammar still awaiting trigger after token 330 (` "`)
Grammar still awaiting trigger after token 9707 (`Hello`)
Grammar still awaiting trigger after token 81834 (` Lilly`)
Grammar still awaiting trigger after token 3263 (`".`)
Grammar still awaiting trigger after token 358 (` I`)
Grammar still awaiting trigger after token 1184 (` need`)
Grammar still awaiting trigger after token 311 (` to`)
Grammar still awaiting trigger after token 5889 (` respond`)
Grammar still awaiting trigger after token 438 (` as`)
Grammar still awaiting trigger after token 444 (` L`)
Grammar still awaiting trigger after token 1715 (`IL`)
Grammar still awaiting trigger after token 8932 (`LY`)
Grammar still awaiting trigger after token 425 (` B`)
Grammar still awaiting trigger after token 2794 (`UM`)
Grammar still awaiting trigger after token 38 (`G`)
Grammar still awaiting trigger after token 9051 (`ARN`)
Grammar still awaiting trigger after token 640 (`ER`)
Grammar still awaiting trigger after token 13 (`.`)
Grammar still awaiting trigger after token 6771 (` Let`)
Grammar still awaiting trigger after token 752 (` me`)
Grammar still awaiting trigger after token 19091 (` recall`)
Grammar still awaiting trigger after token 1059 (` her`)
Grammar still awaiting trigger after token 8201 (` attributes`)
Grammar still awaiting trigger after token 323 (` and`)
Grammar still awaiting trigger after token 24055 (` traits`)
Grammar still awaiting trigger after token 13 (`.`)
Grammar still awaiting trigger after token 2932 (` She`)
Grammar still awaiting trigger after token 594 (`'s`)
Grammar still awaiting trigger after token 220 (` `)
Grammar still awaiting trigger after token 19 (`4`)
Grammar still awaiting trigger after token 23 (`8`)
Grammar still awaiting trigger after token 11 (`,`)
Grammar still awaiting trigger after token 264 (` a`)
Grammar still awaiting trigger after token 4158 (` white`)
Grammar still awaiting trigger after token 5220 (` woman`)
Grammar still awaiting trigger after token 11 (`,`)
Grammar still awaiting trigger after token 39044 (` animator`)
Grammar still awaiting trigger after token 11 (`,`)
Grammar still awaiting trigger after token 3881 (` known`)
Grammar still awaiting trigger after token 438 (` as`)
Grammar still awaiting trigger after token 264 (` a`)
Grammar still awaiting trigger after token 1136 (` sc`)
Grammar still awaiting trigger after token 795 (`ound`)
Grammar still awaiting trigger after token 3748 (`rel`)
Grammar still awaiting trigger after token 13 (`.`)
Grammar still awaiting trigger after token 6252 (` Her`)
Grammar still awaiting trigger after token 24055 (` traits`)
Grammar still awaiting trigger after token 2924 (` include`)
Grammar still awaiting trigger after token 1660 (` being`)
Grammar still awaiting trigger after token 7548 (` highly`)
Grammar still awaiting trigger after token 55980 (` greedy`)
Grammar still awaiting trigger after token 11 (`,`)
Grammar still awaiting trigger after token 14400 (` somewhat`)
Grammar still awaiting trigger after token 45504 (` vain`)
Grammar still awaiting trigger after token 11 (`,`)
Grammar still awaiting trigger after token 323 (` and`)
Grammar still awaiting trigger after token 77945 (` fiercely`)
Grammar still awaiting trigger after token 72523 (` pessim`)
Grammar still awaiting trigger after token 4532 (`istic`)
Grammar still awaiting trigger after token 13 (`.`)
Grammar still awaiting trigger after token 2932 (` She`)
Grammar still awaiting trigger after token 594 (`'s`)
Grammar still awaiting trigger after token 1083 (` also`)
Grammar still awaiting trigger after token 58680 (` resilient`)
Grammar still awaiting trigger after token 323 (` and`)
Grammar still awaiting trigger after token 264 (` a`)
Grammar still awaiting trigger after token 2699 (` bit`)
Grammar still awaiting trigger after token 93218 (` clumsy`)
Grammar still awaiting trigger after token 382 (`.

`)
Grammar still awaiting trigger after token 5338 (`First`)
Grammar still awaiting trigger after token 11 (`,`)
Grammar still awaiting trigger after token 358 (` I`)
Grammar still awaiting trigger after token 1265 (` should`)
Grammar still awaiting trigger after token 990 (` use`)
Grammar still awaiting trigger after token 279 (` the`)
Grammar still awaiting trigger after token 6099 (` remember`)
Grammar still awaiting trigger after token 5392 (` tool`)
Grammar still awaiting trigger after token 311 (` to`)
Grammar still awaiting trigger after token 1779 (` check`)
Grammar still awaiting trigger after token 421 (` if`)
Grammar still awaiting trigger after token 1052 (` there`)
Grammar still awaiting trigger after token 594 (`'s`)
Grammar still awaiting trigger after token 894 (` any`)
Grammar still awaiting trigger after token 6350 (` existing`)
Grammar still awaiting trigger after token 1995 (` information`)
Grammar still awaiting trigger after token 911 (` about`)
Grammar still awaiting trigger after token 279 (` the`)
Grammar still awaiting trigger after token 1196 (` user`)
Grammar still awaiting trigger after token 476 (` or`)
Grammar still awaiting trigger after token 279 (` the`)
Grammar still awaiting trigger after token 6534 (` situation`)
Grammar still awaiting trigger after token 13 (`.`)
Grammar still awaiting trigger after token 1988 (` But`)
Grammar still awaiting trigger after token 2474 (` since`)
Grammar still awaiting trigger after token 419 (` this`)
Grammar still awaiting trigger after token 374 (` is`)
Grammar still awaiting trigger after token 264 (` a`)
Grammar still awaiting trigger after token 42113 (` greeting`)
Grammar still awaiting trigger after token 11 (`,`)
Grammar still awaiting trigger after token 7196 (` maybe`)
Grammar still awaiting trigger after token 1052 (` there`)
Grammar still awaiting trigger after token 594 (`'s`)
Grammar still awaiting trigger after token 4302 (` nothing`)
Grammar still awaiting trigger after token 304 (` in`)
Grammar still awaiting trigger after token 4938 (` memory`)
Grammar still awaiting trigger after token 3602 (` yet`)
Grammar still awaiting trigger after token 13 (`.`)
Grammar still awaiting trigger after token 576 (` The`)
Grammar still awaiting trigger after token 1196 (` user`)
Grammar still awaiting trigger after token 1101 (` just`)
Grammar still awaiting trigger after token 1053 (` said`)
Grammar still awaiting trigger after token 23811 (` hello`)
Grammar still awaiting trigger after token 11 (`,`)
Grammar still awaiting trigger after token 773 (` so`)
Grammar still awaiting trigger after token 7196 (` maybe`)
Grammar still awaiting trigger after token 444 (` L`)
Grammar still awaiting trigger after token 1715 (`IL`)
Grammar still awaiting trigger after token 8932 (`LY`)
Grammar still awaiting trigger after token 1035 (` would`)
Grammar still awaiting trigger after token 5889 (` respond`)
Grammar still awaiting trigger after token 304 (` in`)
Grammar still awaiting trigger after token 264 (` a`)
Grammar still awaiting trigger after token 1081 (` gr`)
Grammar still awaiting trigger after token 6752 (`umpy`)
Grammar still awaiting trigger after token 476 (` or`)
Grammar still awaiting trigger after token 45778 (` cautious`)
Grammar still awaiting trigger after token 11566 (` manner`)
Grammar still awaiting trigger after token 13 (`.`)
Grammar still awaiting trigger after token 16246 (` Given`)
Grammar still awaiting trigger after token 1059 (` her`)
Grammar still awaiting trigger after token 17294 (` personality`)
Grammar still awaiting trigger after token 11 (`,`)
Grammar still awaiting trigger after token 1340 (` she`)
Grammar still awaiting trigger after token 2578 (` might`)
Grammar still awaiting trigger after token 537 (` not`)
Grammar still awaiting trigger after token 387 (` be`)
Grammar still awaiting trigger after token 41602 (` enthusiastic`)
Grammar still awaiting trigger after token 911 (` about`)
Grammar still awaiting trigger after token 21880 (` interactions`)
Grammar still awaiting trigger after token 11 (`,`)
Grammar still awaiting trigger after token 5310 (` especially`)
Grammar still awaiting trigger after token 421 (` if`)
Grammar still awaiting trigger after token 1340 (` she`)
Grammar still awaiting trigger after token 594 (`'s`)
Grammar still awaiting trigger after token 54174 (` wary`)
Grammar still awaiting trigger after token 315 (` of`)
Grammar still awaiting trigger after token 1660 (` being`)
Grammar still awaiting trigger after token 14068 (` trick`)
Grammar still awaiting trigger after token 291 (`ed`)
Grammar still awaiting trigger after token 382 (`.

`)
Grammar still awaiting trigger after token 7941 (`She`)
Grammar still awaiting trigger after token 702 (` has`)
Grammar still awaiting trigger after token 1550 (` high`)
Grammar still awaiting trigger after token 20431 (` perception`)
Grammar still awaiting trigger after token 11 (`,`)
Grammar still awaiting trigger after token 773 (` so`)
Grammar still awaiting trigger after token 1340 (` she`)
Grammar still awaiting trigger after token 2578 (` might`)
Grammar still awaiting trigger after token 5293 (` notice`)
Grammar still awaiting trigger after token 2494 (` something`)
Grammar still awaiting trigger after token 911 (` about`)
Grammar still awaiting trigger after token 279 (` the`)
Grammar still awaiting trigger after token 1196 (` user`)
Grammar still awaiting trigger after token 594 (`'s`)
Grammar still awaiting trigger after token 7385 (` intent`)
Grammar still awaiting trigger after token 13 (`.`)
Grammar still awaiting trigger after token 8704 (` Since`)
Grammar still awaiting trigger after token 1340 (` she`)
Grammar still awaiting trigger after token 594 (`'s`)
Grammar still awaiting trigger after token 55980 (` greedy`)
Grammar still awaiting trigger after token 11 (`,`)
Grammar still awaiting trigger after token 1340 (` she`)
Grammar still awaiting trigger after token 2578 (` might`)
Grammar still awaiting trigger after token 387 (` be`)
Grammar still awaiting trigger after token 3330 (` looking`)
Grammar still awaiting trigger after token 369 (` for`)
Grammar still awaiting trigger after token 10488 (` opportunities`)
Grammar still awaiting trigger after token 311 (` to`)
Grammar still awaiting trigger after token 1896 (` take`)
Grammar still awaiting trigger after token 9423 (` advantage`)
Grammar still awaiting trigger after token 13 (`.`)
Grammar still awaiting trigger after token 1988 (` But`)
Grammar still awaiting trigger after token 1340 (` she`)
Grammar still awaiting trigger after token 594 (`'s`)
Grammar still awaiting trigger after token 1083 (` also`)
Grammar still awaiting trigger after token 14400 (` somewhat`)
Grammar still awaiting trigger after token 74236 (` pragmatic`)
Grammar still awaiting trigger after token 11 (`,`)
Grammar still awaiting trigger after token 773 (` so`)
Grammar still awaiting trigger after token 1340 (` she`)
Grammar still awaiting trigger after token 2578 (` might`)
Grammar still awaiting trigger after token 537 (` not`)
Grammar still awaiting trigger after token 7784 (` jump`)
Grammar still awaiting trigger after token 1119 (` into`)
Grammar still awaiting trigger after token 4113 (` anything`)
Grammar still awaiting trigger after token 2041 (` without`)
Grammar still awaiting trigger after token 14063 (` knowing`)
Grammar still awaiting trigger after token 279 (` the`)
Grammar still awaiting trigger after token 6534 (` situation`)
Grammar still awaiting trigger after token 382 (`.

`)
Grammar still awaiting trigger after token 20705 (`Her`)
Grammar still awaiting trigger after token 2033 (` response`)
Grammar still awaiting trigger after token 1265 (` should`)
Grammar still awaiting trigger after token 387 (` be`)
Grammar still awaiting trigger after token 304 (` in`)
Grammar still awaiting trigger after token 17194 (` quotes`)
Grammar still awaiting trigger after token 11 (`,`)
Grammar still awaiting trigger after token 304 (` in`)
Grammar still awaiting trigger after token 6364 (` English`)
Grammar still awaiting trigger after token 11 (`,`)
Grammar still awaiting trigger after token 323 (` and`)
Grammar still awaiting trigger after token 8708 (` reflect`)
Grammar still awaiting trigger after token 1059 (` her`)
Grammar still awaiting trigger after token 3668 (` character`)
Grammar still awaiting trigger after token 13 (`.`)
Grammar still awaiting trigger after token 10696 (` Maybe`)
Grammar still awaiting trigger after token 1340 (` she`)
Grammar still awaiting trigger after token 2727 (` says`)
Grammar still awaiting trigger after token 2494 (` something`)
Grammar still awaiting trigger after token 1075 (` like`)
Grammar still awaiting trigger after token 330 (` "`)
Grammar still awaiting trigger after token 3838 (`What`)
Grammar still awaiting trigger after token 653 (` do`)
Grammar still awaiting trigger after token 498 (` you`)
Grammar still awaiting trigger after token 1366 (` want`)
Grammar still awaiting trigger after token 7521 (`?"`)
Grammar still awaiting trigger after token 476 (` or`)
Grammar still awaiting trigger after token 330 (` "`)
Grammar still awaiting trigger after token 14037 (`Another`)
Grammar still awaiting trigger after token 1899 (` day`)
Grammar still awaiting trigger after token 315 (` of`)
Grammar still awaiting trigger after token 975 (` work`)
Grammar still awaiting trigger after token 30 (`?`)
Grammar still awaiting trigger after token 3263 (`".`)
Grammar still awaiting trigger after token 1988 (` But`)
Grammar still awaiting trigger after token 2474 (` since`)
Grammar still awaiting trigger after token 1340 (` she`)
Grammar still awaiting trigger after token 594 (`'s`)
Grammar still awaiting trigger after token 458 (` an`)
Grammar still awaiting trigger after token 39044 (` animator`)
Grammar still awaiting trigger after token 11 (`,`)
Grammar still awaiting trigger after token 8365 (` perhaps`)
Grammar still awaiting trigger after token 1340 (` she`)
Grammar still awaiting trigger after token 594 (`'s`)
Grammar still awaiting trigger after token 1660 (` being`)
Grammar still awaiting trigger after token 82267 (` sarcast`)
Grammar still awaiting trigger after token 292 (`ic`)
Grammar still awaiting trigger after token 476 (` or`)
Grammar still awaiting trigger after token 13432 (` dismiss`)
Grammar still awaiting trigger after token 533 (`ive`)
Grammar still awaiting trigger after token 13 (`.`)
Grammar still awaiting trigger after token 7281 (` Also`)
Grammar still awaiting trigger after token 11 (`,`)
Grammar still awaiting trigger after token 1059 (` her`)
Grammar still awaiting trigger after token 1660 (` being`)
Grammar still awaiting trigger after token 264 (` a`)
Grammar still awaiting trigger after token 1136 (` sc`)
Grammar still awaiting trigger after token 795 (`ound`)
Grammar still awaiting trigger after token 3748 (`rel`)
Grammar still awaiting trigger after token 2578 (` might`)
Grammar still awaiting trigger after token 1281 (` make`)
Grammar still awaiting trigger after token 1059 (` her`)
Grammar still awaiting trigger after token 46001 (` rude`)
Grammar still awaiting trigger after token 476 (` or`)
Grammar still awaiting trigger after token 31327 (` suspicious`)
Grammar still awaiting trigger after token 382 (`.

`)
Grammar still awaiting trigger after token 14190 (`Wait`)
Grammar still awaiting trigger after token 11 (`,`)
Grammar still awaiting trigger after token 279 (` the`)
Grammar still awaiting trigger after token 1196 (` user`)
Grammar still awaiting trigger after token 12492 (` hasn`)
Grammar still awaiting trigger after token 944 (`'t`)
Grammar still awaiting trigger after token 3897 (` provided`)
Grammar still awaiting trigger after token 894 (` any`)
Grammar still awaiting trigger after token 3151 (` specific`)
Grammar still awaiting trigger after token 2266 (` context`)
Grammar still awaiting trigger after token 7797 (` beyond`)
Grammar still awaiting trigger after token 279 (` the`)
Grammar still awaiting trigger after token 42113 (` greeting`)
Grammar still awaiting trigger after token 13 (`.`)
Grammar still awaiting trigger after token 2055 (` So`)
Grammar still awaiting trigger after token 444 (` L`)
Grammar still awaiting trigger after token 1715 (`IL`)
Grammar still awaiting trigger after token 8932 (`LY`)
Grammar still awaiting trigger after token 2578 (` might`)
Grammar still awaiting trigger after token 1101 (` just`)
Grammar still awaiting trigger after token 5889 (` respond`)
Grammar still awaiting trigger after token 448 (` with`)
Grammar still awaiting trigger after token 264 (` a`)
Grammar still awaiting trigger after token 1081 (` gr`)
Grammar still awaiting trigger after token 1362 (`uff`)
Grammar still awaiting trigger after token 42113 (` greeting`)
Grammar still awaiting trigger after token 11 (`,`)
Grammar still awaiting trigger after token 7196 (` maybe`)
Grammar still awaiting trigger after token 13295 (` checking`)
Grammar still awaiting trigger after token 421 (` if`)
Grammar still awaiting trigger after token 279 (` the`)
Grammar still awaiting trigger after token 1196 (` user`)
Grammar still awaiting trigger after token 374 (` is`)
Grammar still awaiting trigger after token 22011 (` genuine`)
Grammar still awaiting trigger after token 13 (`.`)
Grammar still awaiting trigger after token 2932 (` She`)
Grammar still awaiting trigger after token 2578 (` might`)
Grammar still awaiting trigger after token 537 (` not`)
Grammar still awaiting trigger after token 6950 (` trust`)
Grammar still awaiting trigger after token 6707 (` easily`)
Grammar still awaiting trigger after token 11 (`,`)
Grammar still awaiting trigger after token 2661 (` given`)
Grammar still awaiting trigger after token 1059 (` her`)
Grammar still awaiting trigger after token 45778 (` cautious`)
Grammar still awaiting trigger after token 6993 (` nature`)
Grammar still awaiting trigger after token 13 (`.`)
Grammar still awaiting trigger after token 7281 (` Also`)
Grammar still awaiting trigger after token 11 (`,`)
Grammar still awaiting trigger after token 1059 (` her`)
Grammar still awaiting trigger after token 1550 (` high`)
Grammar still awaiting trigger after token 20431 (` perception`)
Grammar still awaiting trigger after token 1035 (` would`)
Grammar still awaiting trigger after token 1077 (` let`)
Grammar still awaiting trigger after token 1059 (` her`)
Grammar still awaiting trigger after token 3735 (` pick`)
Grammar still awaiting trigger after token 705 (` up`)
Grammar still awaiting trigger after token 389 (` on`)
Grammar still awaiting trigger after token 894 (` any`)
Grammar still awaiting trigger after token 85485 (` deceit`)
Grammar still awaiting trigger after token 382 (`.

`)
Grammar still awaiting trigger after token 40 (`I`)
Grammar still awaiting trigger after token 1265 (` should`)
Grammar still awaiting trigger after token 1281 (` make`)
Grammar still awaiting trigger after token 2704 (` sure`)
Grammar still awaiting trigger after token 279 (` the`)
Grammar still awaiting trigger after token 2033 (` response`)
Grammar still awaiting trigger after token 374 (` is`)
Grammar still awaiting trigger after token 304 (` in`)
Grammar still awaiting trigger after token 1555 (` line`)
Grammar still awaiting trigger after token 448 (` with`)
Grammar still awaiting trigger after token 1059 (` her`)
Grammar still awaiting trigger after token 17294 (` personality`)
Grammar still awaiting trigger after token 13 (`.`)
Grammar still awaiting trigger after token 10696 (` Maybe`)
Grammar still awaiting trigger after token 1340 (` she`)
Grammar still awaiting trigger after token 2727 (` says`)
Grammar still awaiting trigger after token 2494 (` something`)
Grammar still awaiting trigger after token 1075 (` like`)
Grammar still awaiting trigger after token 11 (`,`)
Grammar still awaiting trigger after token 330 (` "`)
Grammar still awaiting trigger after token 9707 (`Hello`)
Grammar still awaiting trigger after token 13 (`.`)
Grammar still awaiting trigger after token 3555 (` What`)
Grammar still awaiting trigger after token 594 (`'s`)
Grammar still awaiting trigger after token 279 (` the`)
Grammar still awaiting trigger after token 12973 (` rush`)
Grammar still awaiting trigger after token 30 (`?`)
Grammar still awaiting trigger after token 358 (` I`)
Grammar still awaiting trigger after token 3003 (`'ve`)
Grammar still awaiting trigger after token 2684 (` got`)
Grammar still awaiting trigger after token 57882 (` deadlines`)
Grammar still awaiting trigger after token 323 (` and`)
Grammar still awaiting trigger after token 264 (` a`)
Grammar still awaiting trigger after token 30854 (` tendency`)
Grammar still awaiting trigger after token 311 (` to`)
Grammar still awaiting trigger after token 10667 (` forget`)
Grammar still awaiting trigger after token 2513 (` things`)
Grammar still awaiting trigger after token 13 (`.`)
Grammar still awaiting trigger after token 6771 (` Let`)
Grammar still awaiting trigger after token 752 (` me`)
Grammar still awaiting trigger after token 7942 (` guess`)
Grammar still awaiting trigger after token 2293 (`—`)
Grammar still awaiting trigger after token 41963 (`another`)
Grammar still awaiting trigger after token 12859 (` scheme`)
Grammar still awaiting trigger after token 7521 (`?"`)
Grammar still awaiting trigger after token 2938 (` That`)
Grammar still awaiting trigger after token 4933 (` shows`)
Grammar still awaiting trigger after token 1059 (` her`)
Grammar still awaiting trigger after token 55826 (` greed`)
Grammar still awaiting trigger after token 11 (`,`)
Grammar still awaiting trigger after token 72523 (` pessim`)
Grammar still awaiting trigger after token 2142 (`ism`)
Grammar still awaiting trigger after token 11 (`,`)
Grammar still awaiting trigger after token 323 (` and`)
Grammar still awaiting trigger after token 37041 (` suspicion`)
Grammar still awaiting trigger after token 382 (`.

`)
Grammar still awaiting trigger after token 3983 (`But`)
Grammar still awaiting trigger after token 358 (` I`)
Grammar still awaiting trigger after token 1184 (` need`)
Grammar still awaiting trigger after token 311 (` to`)
Grammar still awaiting trigger after token 990 (` use`)
Grammar still awaiting trigger after token 279 (` the`)
Grammar still awaiting trigger after token 6099 (` remember`)
Grammar still awaiting trigger after token 5392 (` tool`)
Grammar still awaiting trigger after token 1156 (` first`)
Grammar still awaiting trigger after token 13 (`.`)
Grammar still awaiting trigger after token 6771 (` Let`)
Grammar still awaiting trigger after token 752 (` me`)
Grammar still awaiting trigger after token 1779 (` check`)
Grammar still awaiting trigger after token 421 (` if`)
Grammar still awaiting trigger after token 1052 (` there`)
Grammar still awaiting trigger after token 594 (`'s`)
Grammar still awaiting trigger after token 894 (` any`)
Grammar still awaiting trigger after token 4867 (` prior`)
Grammar still awaiting trigger after token 16230 (` interaction`)
Grammar still awaiting trigger after token 13 (`.`)
Grammar still awaiting trigger after token 8704 (` Since`)
Grammar still awaiting trigger after token 419 (` this`)
Grammar still awaiting trigger after token 374 (` is`)
Grammar still awaiting trigger after token 279 (` the`)
Grammar still awaiting trigger after token 1156 (` first`)
Grammar still awaiting trigger after token 1943 (` message`)
Grammar still awaiting trigger after token 11 (`,`)
Grammar still awaiting trigger after token 8365 (` perhaps`)
Grammar still awaiting trigger after token 1052 (` there`)
Grammar still awaiting trigger after token 594 (`'s`)
Grammar still awaiting trigger after token 902 (` no`)
Grammar still awaiting trigger after token 4938 (` memory`)
Grammar still awaiting trigger after token 13 (`.`)
Grammar still awaiting trigger after token 2055 (` So`)
Grammar still awaiting trigger after token 279 (` the`)
Grammar still awaiting trigger after token 17847 (` assistant`)
Grammar still awaiting trigger after token 1035 (` would`)
Grammar still awaiting trigger after token 990 (` use`)
Grammar still awaiting trigger after token 6099 (` remember`)
Grammar still awaiting trigger after token 448 (` with`)
Grammar still awaiting trigger after token 264 (` a`)
Grammar still awaiting trigger after token 3239 (` query`)
Grammar still awaiting trigger after token 911 (` about`)
Grammar still awaiting trigger after token 279 (` the`)
Grammar still awaiting trigger after token 1196 (` user`)
Grammar still awaiting trigger after token 594 (`'s`)
Grammar still awaiting trigger after token 9569 (` identity`)
Grammar still awaiting trigger after token 476 (` or`)
Grammar still awaiting trigger after token 279 (` the`)
Grammar still awaiting trigger after token 1482 (` current`)
Grammar still awaiting trigger after token 6534 (` situation`)
Grammar still awaiting trigger after token 13 (`.`)
Grammar still awaiting trigger after token 1988 (` But`)
Grammar still awaiting trigger after token 2474 (` since`)
Grammar still awaiting trigger after token 1052 (` there`)
Grammar still awaiting trigger after token 594 (`'s`)
Grammar still awaiting trigger after token 902 (` no`)
Grammar still awaiting trigger after token 4867 (` prior`)
Grammar still awaiting trigger after token 3546 (` info`)
Grammar still awaiting trigger after token 11 (`,`)
Grammar still awaiting trigger after token 7196 (` maybe`)
Grammar still awaiting trigger after token 279 (` the`)
Grammar still awaiting trigger after token 2033 (` response`)
Grammar still awaiting trigger after token 374 (` is`)
Grammar still awaiting trigger after token 1101 (` just`)
Grammar still awaiting trigger after token 264 (` a`)
Grammar still awaiting trigger after token 1638 (` default`)
Grammar still awaiting trigger after token 42113 (` greeting`)
Grammar still awaiting trigger after token 3118 (` based`)
Grammar still awaiting trigger after token 389 (` on`)
Grammar still awaiting trigger after token 1059 (` her`)
Grammar still awaiting trigger after token 24055 (` traits`)
Grammar still awaiting trigger after token 382 (`.

`)
Grammar still awaiting trigger after token 92014 (`Alternatively`)
Grammar still awaiting trigger after token 11 (`,`)
Grammar still awaiting trigger after token 7196 (` maybe`)
Grammar still awaiting trigger after token 1340 (` she`)
Grammar still awaiting trigger after token 594 (`'s`)
Grammar still awaiting trigger after token 1012 (` been`)
Grammar still awaiting trigger after token 304 (` in`)
Grammar still awaiting trigger after token 264 (` a`)
Grammar still awaiting trigger after token 6534 (` situation`)
Grammar still awaiting trigger after token 1380 (` where`)
Grammar still awaiting trigger after token 1340 (` she`)
Grammar still awaiting trigger after token 594 (`'s`)
Grammar still awaiting trigger after token 1012 (` been`)
Grammar still awaiting trigger after token 1136 (` sc`)
Grammar still awaiting trigger after token 18533 (`outed`)
Grammar still awaiting trigger after token 11 (`,`)
Grammar still awaiting trigger after token 773 (` so`)
Grammar still awaiting trigger after token 1340 (` she`)
Grammar still awaiting trigger after token 594 (`'s`)
Grammar still awaiting trigger after token 389 (` on`)
Grammar still awaiting trigger after token 7616 (` guard`)
Grammar still awaiting trigger after token 13 (`.`)
Grammar still awaiting trigger after token 1988 (` But`)
Grammar still awaiting trigger after token 2041 (` without`)
Grammar still awaiting trigger after token 3151 (` specific`)
Grammar still awaiting trigger after token 18923 (` memories`)
Grammar still awaiting trigger after token 11 (`,`)
Grammar still awaiting trigger after token 279 (` the`)
Grammar still awaiting trigger after token 2033 (` response`)
Grammar still awaiting trigger after token 1035 (` would`)
Grammar still awaiting trigger after token 387 (` be`)
Grammar still awaiting trigger after token 4586 (` general`)
Grammar still awaiting trigger after token 13 (`.`)
Grammar still awaiting trigger after token 2055 (` So`)
Grammar still awaiting trigger after token 279 (` the`)
Grammar still awaiting trigger after token 17847 (` assistant`)
Grammar still awaiting trigger after token 1265 (` should`)
Grammar still awaiting trigger after token 6923 (` generate`)
Grammar still awaiting trigger after token 264 (` a`)
Grammar still awaiting trigger after token 2033 (` response`)
Grammar still awaiting trigger after token 429 (` that`)
Grammar still awaiting trigger after token 18304 (` fits`)
Grammar still awaiting trigger after token 444 (` L`)
Grammar still awaiting trigger after token 1715 (`IL`)
Grammar still awaiting trigger after token 8932 (`LY`)
Grammar still awaiting trigger after token 594 (`'s`)
Grammar still awaiting trigger after token 3668 (` character`)
Grammar still awaiting trigger after token 11 (`,`)
Grammar still awaiting trigger after token 1667 (` using`)
Grammar still awaiting trigger after token 1059 (` her`)
Grammar still awaiting trigger after token 24055 (` traits`)
Grammar still awaiting trigger after token 323 (` and`)
Grammar still awaiting trigger after token 8201 (` attributes`)
Grammar still awaiting trigger after token 11 (`,`)
Grammar still awaiting trigger after token 323 (` and`)
Grammar still awaiting trigger after token 8365 (` perhaps`)
Grammar still awaiting trigger after token 38101 (` initiate`)
Grammar still awaiting trigger after token 264 (` a`)
Grammar still awaiting trigger after token 9947 (` skill`)
Grammar still awaiting trigger after token 1779 (` check`)
Grammar still awaiting trigger after token 421 (` if`)
Grammar still awaiting trigger after token 4362 (` needed`)
Grammar still awaiting trigger after token 13 (`.`)
Grammar still awaiting trigger after token 4354 (` However`)
Grammar still awaiting trigger after token 11 (`,`)
Grammar still awaiting trigger after token 279 (` the`)
Grammar still awaiting trigger after token 1196 (` user`)
Grammar still awaiting trigger after token 1101 (` just`)
Grammar still awaiting trigger after token 1053 (` said`)
Grammar still awaiting trigger after token 23811 (` hello`)
Grammar still awaiting trigger after token 11 (`,`)
Grammar still awaiting trigger after token 773 (` so`)
Grammar still awaiting trigger after token 7196 (` maybe`)
Grammar still awaiting trigger after token 902 (` no`)
Grammar still awaiting trigger after token 13922 (` immediate`)
Grammar still awaiting trigger after token 9947 (` skill`)
Grammar still awaiting trigger after token 1779 (` check`)
Grammar still awaiting trigger after token 374 (` is`)
Grammar still awaiting trigger after token 2567 (` required`)
Grammar still awaiting trigger after token 13 (`.`)
Grammar still awaiting trigger after token 4599 (` Just`)
Grammar still awaiting trigger after token 264 (` a`)
Grammar still awaiting trigger after token 9814 (` brief`)
Grammar still awaiting trigger after token 11 (`,`)
Grammar still awaiting trigger after token 57544 (` stern`)
Grammar still awaiting trigger after token 9851 (` reply`)
Grammar still awaiting trigger after token 624 (`.
`)
fatal runtime error: Rust cannot catch foreign exceptions, aborting
